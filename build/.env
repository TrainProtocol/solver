# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=solver.db
POSTGRES_PORT=5432

# Temporal Configuration
TEMPORAL_SERVER_HOST=temporal.server:7233
TEMPORAL_DB_NAME=temporal
TEMPORAL_VISIBILITY_DB_NAME=temporal_visibility

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Azure Configuration
AZURE_KEYVAULT_URI=https://placeholder.vault.azure.net/

# Application Configuration
TRAIN_SOLVER_DB_CONNECTION_STRING=Host=db;Port=${POSTGRES_PORT};Database=${POSTGRES_DB};User ID=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
TRAIN_SOLVER_REDIS_CONNECTION_STRING=${REDIS_HOST}:${REDIS_PORT} 